{
  "name": "llamafactory-startos",
  "version": "0.1.0",
  "description": "StartOS service: orchestrate LLaMA-Factory fine-tunes on remote DGX Sparks",
  "scripts": {
    "build": "rm -rf ./javascript && ncc build startos/index.ts -o ./javascript",
    "check": "tsc --noEmit",
    "prettier": "prettier --write startos"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@start9labs/start-sdk": "1.5.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "@vercel/ncc": "^0.38.4",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
